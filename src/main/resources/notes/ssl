#generate keystore
keytool -genkey -alias wsiztest -storetype PKCS12 -keyalg RSA -keysize 2048 -keystore keystore.p12 -validity 10000

#the keystore.p12 must be in root of classpath (so: in same dir as app.jar, launched via java -jar app.jar)

